<div class="container">
    <div class="row">
        <div class="col-md-3">
        <%= render'item', item: @item %>
        </div>
        <div class="col-md-9">
            <h3>商品一覧(全　件)</h3>
            <% @items.each do |item| %>
            <%= link_to public_item_path(item.id) do %>
                <%= image_tag item.image, size:"150x150" %>
            <% end %>
            <%= link_to item.name, public_item_path(item.id) %>
            ¥<%= item.price %>
            <% end %>
            <p class="text-center"><%= paginate @items %></p>
        </div>
    </div>
</div>